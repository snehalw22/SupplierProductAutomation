package com.supplyhouse.dao;

import com.supplyhouse.supplier.SupplierProduct;
/**
 * @author Snehal
 * An interface implementation of SupplierProductDao which implements the abstract methods
 * to perform database related operations on SupplierProduct
 */
class SupplierProductDaoImpl implements SupplierProductDao{

	@Override
	public void updateSupplierProduct(SupplierProduct supplierProduct) {
		//get the connection from the ConnectionManager and update the supplier		
	}

	@Override
	public void insertSupplierProduct(SupplierProduct supplierProduct) {
		//get the connection from the ConnectionManager and insert the supplier		
		
	}
}
